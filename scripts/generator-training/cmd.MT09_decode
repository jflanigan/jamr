#!/usr/bin/env bash

INPUT="${MODEL_DIR}/MT09-grammars.snt"
OUTPUT="${MODEL_DIR}/MT09_decode"

"${JAMR_HOME}/tools/cdec/decoder/cdec" \
    -c "${MODEL_DIR}/dpmert/cdec.ini" \
    -w "${MODEL_DIR}/dpmert/weights" \
    -i "${INPUT}" \
    > "${OUTPUT}.txt" \
    2> "${OUTPUT}.err"

