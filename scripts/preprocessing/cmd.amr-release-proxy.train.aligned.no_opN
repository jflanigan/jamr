#!/bin/bash

cat "${DATA_DIR}/amr-release-proxy.train.aligned" | sed 's/:op[^ ]*/:op/g' > "${DATA_DIR}/amr-release-proxy.train.aligned.no_opN"
cat "${DATA_DIR}/amr-release-proxy.dev.aligned" | sed 's/:op[^ ]*/:op/g' > "${DATA_DIR}/amr-release-proxy.dev.aligned.no_opN"
cat "${DATA_DIR}/amr-release-proxy.test.aligned" | sed 's/:op[^ ]*/:op/g' > "${DATA_DIR}/amr-release-proxy.test.aligned.no_opN"
